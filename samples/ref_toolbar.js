/* global angular */

(function () {
    'use strict';

    var thisModule = angular.module('appComponentsStyles.RefToolbar', []);

    thisModule.controller('RefToolbarController',
        function($scope) {
        }
    );

})();
